import React from 'react';
import Header from '../Header/Header';

const NoMatch = () => {
    return (
        <div>
            
            <h1>No routes found</h1>
        </div>
    );
};

export default NoMatch;